
// React uses JSX (JavaScript XML) to describe the UI. JSX looks like HTML but is written in JavaScript.

// 1. JSX allows us to write HTML-like syntax directly in JavaScript.
// 2. It must be returned inside a function component or class component in React.

function JsxBasic() {
    return <h1>Hello, World!</h1>;
}

export default JsxBasic;
